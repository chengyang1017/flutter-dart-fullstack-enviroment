name: flutterpractice_cli
description: ApplyKit CLI for creating and applying portable project change packages.
publish_to: none
version: 0.2.0

environment:
  sdk: ^3.4.0

executables:
  applykit:

dependencies:
  archive: ^4.2.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0